# Prototype Notes

Question: does the run-card state model make Karl feel like "the dude" for
agent callers, instead of making the caller remember OpenRouter/router/tooling
trivia?

Verdict:

- TUI was the wrong first shell. Most Karl callers are agents, so the prototype
  should behave like a planning endpoint: deterministic text or JSON in/stdout,
  with explicit route selection and receipt fields.
- Fill this in after driving the prototype.
- Delete the prototype shell when the useful shape has been absorbed into real
  Karl code.
